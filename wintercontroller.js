var app = angular.module("mySeasons");

app.controller('$scope', ['winterCtrl', function($scope){
	$scope.checkForPalindrome = function(palindrome) {
		//string value ex.james
		// split string into array
		//forEach alphabet push it into an empty array
		//empty array will now be a new variable ex.semaj
		//if james === semaj return, "Yes, it is a Palindrome!"
	}
}]);
